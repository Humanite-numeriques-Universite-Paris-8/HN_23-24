export class Auth {
    constructor(params) {
        this.mail = params.mail;
        this.apiOmk = params.apiOmk;
        this.ident = params.ident;
        this.key = params.key;
    }

    // Méthodes d'authentification si nécessaire
}
